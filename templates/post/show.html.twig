{% extends 'base.html.twig' %}
{% block title %}{{ post.title }}{% endblock %}

{% block body %}
<div class="container mt-4">
    <h2>{{ post.title }}</h2>
    <p class="text-muted">
        Author: {{ post.author.email }} |
        Date: {{ post.createdAt|date('Y-m-d H:i') }}
    </p>
    <hr>
    <div>
        {{ post.contentHtml|raw }}
    </div>
    <a href="{{ path('post_list') }}" class="btn btn-secondary mt-3">← Back to list</a>
</div>
{% endblock %}
